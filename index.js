//a loop that check each value of the string is only repeated once, if it repeated more than once than ransomNote=false and cannot be constructed. 

//Basically meaning the ransomnote cannot be created using the message in the magazine.


x=magazine.length

for (i=0; i<= x; i++)

if (magazine[i] = ransom[i])
{//Code that counts amount of time that character appears
//if that character is count is greater than 1 than break the loop and console.log('This cannot be a ransom note.)

}
